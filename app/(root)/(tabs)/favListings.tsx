import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const FavListings = () => {
    return (
        <SafeAreaView className=''>
            <Text>FavListings</Text>
        </SafeAreaView>
    );
}

export default FavListings;